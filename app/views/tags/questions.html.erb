<h1>Questions tagged with "<%= @tag.name %>"</h1>

<table border="1">
  <tr>
    <th>id</th>
    <th>単語</th>
    <!-- 他の質問に関する情報をここに追加 -->
  </tr>
  <% @questions.each do |question| %>
    <tr>
      <td><%= question.id %></td>
      <td><%= question.title %></td>
      <!-- 他の質問に関する情報をここに追加 -->
    </tr>
  <% end %>
</table>